const assert = require("assert");
const fpTetris = require("../index.js");

describe("fpTetris", () => {
  it("has a test", () => {
    assert(fpTetris !== undefined, "fpTetris should have a test");
  });
});
